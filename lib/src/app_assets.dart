class AppAssets {
  static const String logo = 'assets/images/logo.png';

  /// Wordmark used on the splash. Declared here so the path lives in one
  /// place; the splash degrades to a text wordmark if the file is missing,
  /// so a fresh checkout without it still boots.
  static const String slogan = 'assets/images/slogan.png';

  static const String cursor = 'assets/images/cursor.png';
}
